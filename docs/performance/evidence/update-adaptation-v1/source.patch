diff --git c/docs/performance/evidence/update-transaction-v2/README.md w/docs/performance/evidence/update-transaction-v2/README.md
index 7dd4737..c44f199 100644
--- c/docs/performance/evidence/update-transaction-v2/README.md
+++ w/docs/performance/evidence/update-transaction-v2/README.md
@@ -17,3 +17,5 @@ python3 scripts/benchmark/reproduce-update-publication.py --output /path/to/new-
 Choose two distinct physical cores on another host. Existing output directories are rejected. Generated PDF and these identifying provenance artifacts must not be submitted together for anonymous review.
 
 `validation/` retains separate unit, Loom, cross-compilation and QEMU checks. The QEMU process was intentionally stopped by a 55-second timeout (exit 124) after the replacement/interpreter and later BPF smoke markers passed; it is an idle boot, not a terminating test. These checks are separate from the 80-run hosted experiment. No source change was required after the final checks.
+
+The QEMU marker `interpreter_a_b_a=true` checks one interpreter dispatch after each A/B/A installation; it does not compare distinct returned controller values. Installation receipts establish the sequence. Identity/runtime coupling additionally follows from constructing one immutable payload with the candidate handle and that candidate's runtime; the marker is not a separate measurement of returned-value identity.
diff --git c/docs/performance/evidence/update-transaction-v2/SHA256SUMS w/docs/performance/evidence/update-transaction-v2/SHA256SUMS
index 5a4836f..6fe0318 100644
--- c/docs/performance/evidence/update-transaction-v2/SHA256SUMS
+++ w/docs/performance/evidence/update-transaction-v2/SHA256SUMS
@@ -1,4 +1,4 @@
-76b13e7c69512ed142c5e706135246622ac67e1b6dd4637bd63dde38b25057d8  README.md
+cb9c7d2f8f8cf28fcf658b0bb4f971406799f383fc4eb2b1d9b3641249a4692c  README.md
 89a7f1161fc4e3f8070f4a549f4e37ea1ef0b8c5db5da3fd157afec0287e027a  analysis.json
 f8da316be7adb876b091b4b6aa200b2022095121857ed4b3639f4490641c094a  build-artifacts.jsonl
 88f5f717f6176a7b07dee882a064a5fe91f7a23ce8cd2977b60f311f7c792ed1  build-command.json
diff --git c/docs/plans/active/cl4fmagents-transactional-publication.md w/docs/plans/active/cl4fmagents-transactional-publication.md
index f8f487b..c90ee03 100644
--- c/docs/plans/active/cl4fmagents-transactional-publication.md
+++ w/docs/plans/active/cl4fmagents-transactional-publication.md
@@ -47,6 +47,15 @@ Hosted release campaign: holds 0/10/100/500 us, 1 ms dispatch period, 10 fresh p
 
 Optional replay only after core is stable: real manager/guards/receipts select full captured installation IDs, pure-host first-order velocity simulation with payload shift and frozen online-tuner gain proposals. Commands use the identity and error captured at invocation start, apply in completion order, and use the same zero-order hold policy. No LLM, new runtime API, physical claims, or stateful controller. Start only if core stable by Sep 8 07:29 IST; validated by 13:29; final PDF freeze 15:29; otherwise omit.
 
+## v2 core completed
+
+- Local commit `8c42d3e` adds the shared-check atomic baseline, paired manager campaign, hosted cost matrix, artifact integrity checks, and revised four-page paper. No publication or remote branch action.
+- Retained `update-transaction-v2`: 28 correctness records; 80 fresh cost processes, 80,000 replacement attempts, 244,364 cost records. Source and measured executable hashes remained unchanged during capture.
+- AP and GR each expose one current installation at the ten observed paired checkpoints; the held-predecessor schedule exposes one AP old/new guard overlap and none for GR. Both preserve six rejecting requests. AF/DF remain supporting witnesses.
+- At 500 us hold, AP completes all 10,000 attempts and GR completes 4,982, with 95 GR natural dispatch skips. Full counts, retries, latency distributions, and scheduler misses are retained; no real-time or task-performance claim.
+- Manager, eight Loom cases, BPF tests, embedded/production cross-checks, QEMU interpreter probe and 17 Python oracle/collector tests pass. Separate logs are retained under the v2 evidence directory.
+- Four content pages plus references pass page/anonymity/provenance/table checks; all five pages visually inspected. Core PDF fallback retained in the shared target directory. Optional simulation replay started after core stabilization, before the accepted cutoff.
+
 ## v1 progress
 
 - Branch/worktree created; original checkout preserved; paper draft copied.
diff --git c/papers/cl4fmagents2026/Makefile w/papers/cl4fmagents2026/Makefile
index c948704..bdfe701 100644
--- c/papers/cl4fmagents2026/Makefile
+++ w/papers/cl4fmagents2026/Makefile
@@ -4,8 +4,11 @@ PDF := who-guards-the-update.pdf
 .PHONY: all
 all:
 	mkdir -p $(OUT)
+	sha256sum Makefile *.tex *.bib *.sty > $(OUT)/inputs.sha256
 	pdflatex -interaction=nonstopmode -halt-on-error -output-directory=$(OUT) main.tex > $(OUT)/compile.log
 	cd $(OUT) && BIBINPUTS=..: bibtex main >> compile.log
 	pdflatex -interaction=nonstopmode -halt-on-error -output-directory=$(OUT) main.tex >> $(OUT)/compile.log
 	pdflatex -interaction=nonstopmode -halt-on-error -output-directory=$(OUT) main.tex >> $(OUT)/compile.log
 	cp $(OUT)/main.pdf $(PDF)
+	sha256sum -c $(OUT)/inputs.sha256 >> $(OUT)/compile.log
+	sha256sum $(PDF) >> $(OUT)/inputs.sha256
diff --git c/papers/cl4fmagents2026/README.md w/papers/cl4fmagents2026/README.md
index b7aa5d5..1fd8d7e 100644
--- c/papers/cl4fmagents2026/README.md
+++ w/papers/cl4fmagents2026/README.md
@@ -7,18 +7,26 @@ make
 python3 verify.py
 ```
 
-Output: `who-guards-the-update.pdf`. Upload that PDF only. The ignored `private/` directory and repository evidence contain author-facing information and are not anonymous review material. The paper reports software publication correctness, not learning quality or physical safety.
+Output: `who-guards-the-update.pdf`. Upload that PDF only. The ignored `private/` directory and repository evidence contain author-facing information and are not anonymous review material. The paper reports software publication correctness, hosted costs, and a deterministic simulated control replay. It does not evaluate foundation-model learning or physical safety.
 
 The v1 campaign remains in `docs/performance/evidence/update-transaction`. The strengthened campaign uses a new directory, `update-transaction-v2`, and compares guarded replacement with a host-only atomic publication path sharing the same manager checks. Host callbacks retain real invocation guards but do not execute privileged bytecode.
 
-From the repository root, reproduce the release correctness campaign, 80 fresh-process measurement runs, analysis tables, and paper checks:
+From the repository root, reproduce the release correctness campaign, 80 fresh-process measurement runs, separate adaptation replay, generated tables, and paper checks:
 
 ```sh
 CARGO_TARGET_DIR=/home/utkarsh/Work/axiomOS/target python3 scripts/benchmark/reproduce-update-publication.py --output /tmp/update-publication-reproduction
 ```
 
-Choose an existing writable Cargo target directory with sufficient disk space on other machines. The output directory must not exist. The measurement driver records affinity and requires two distinct physical cores; its defaults are CPU 12 and 14 on the developer host. No scheduler or governor settings are changed. The retained manifest hashes source and measured executables. Raw scheduler misses and latency tails are retained.
+Choose an existing writable Cargo target directory with sufficient disk space on other machines. The output directory and its `<output>-adaptation` sibling must not exist. The measurement driver records affinity and requires two distinct physical cores; its defaults are CPU 12 and 14 on the developer host. No scheduler or governor settings are changed. The retained manifest hashes source and measured executables. Raw scheduler misses and latency tails are retained.
 
-`results.tex` and `costs.tex` are generated from raw traces. Verification checks the PDF page limit, anonymity, citations/build warnings, and generated tables. Visually inspect every rendered page after layout changes. These artifacts contain developer-identifying provenance and must not accompany the anonymous PDF.
+`results.tex`, `costs.tex`, `cost-note.tex`, and `adaptation.tex` are generated from raw traces. Verification checks source/PDF build hashes, page limit, anonymity, citations/build warnings, artifact provenance, and trace-derived tables. Visually inspect every rendered page after layout changes. These artifacts contain developer-identifying provenance and must not accompany the anonymous PDF.
 
 `neurips_2026.sty` is the unmodified official 2026 style. The manuscript uses `dblblindworkshop` and supplies the workshop title. No OpenReview submission receipt has been obtained.
+
+The separate replay evidence is `docs/performance/evidence/update-adaptation-v1`. Its fixed candidate sequence is generated once by a feedback tuner and replayed through real manager installations and host guard callbacks. The plant and control arithmetic are simulated; callbacks do not execute privileged BPF code. A local paired utility probe compares gains from identical activation state/time, separately from aggregate before/after tracking RMSE. One deliberately long invocation exposes the difference between pointer publication and invocation quiescence; it is a finite witness, not an estimated failure rate.
+
+To reproduce only the replay into a new directory:
+
+```sh
+CARGO_TARGET_DIR=/home/utkarsh/Work/axiomOS/target python3 scripts/benchmark/reproduce-update-adaptation.py --output /tmp/update-adaptation-reproduction
+```
diff --git c/papers/cl4fmagents2026/main.tex w/papers/cl4fmagents2026/main.tex
index dbb6fe1..dbdcd38 100644
--- c/papers/cl4fmagents2026/main.tex
+++ w/papers/cl4fmagents2026/main.tex
@@ -14,7 +14,7 @@
 \maketitle
 
 \begin{abstract}
-An embodied agent can improve a controller while remaining unreliable at installing it. Atomic publication selects one current program, but does not by itself prevent old and new invocations from overlapping. We define an executor-level installation contract combining expected installation identity, invocation quiescence, stateless admission, authority revalidation, and resource-atomic publication. A prototype implements this contract for one opt-in runtime slot using a nonblocking transition gate. We compare it with atomic publication sharing the same manager checks, accounting, and receipts, under matched failures and concurrent invocation. Across nine matched scenarios, atomic publication exposes one old/new-guard overlap; guarded replacement exposes none. Both preserve the predecessor on six rejecting requests. Hosted measurements separately quantify replacement and guard costs, retries, skipped dispatches, and retained bytecode. The results concern software installation; closed-loop continual learning, downstream command fencing, and physical safety remain outside the evaluated contract.
+An embodied agent can improve a controller while remaining unreliable at installing it. Atomic publication selects one current program, but does not by itself prevent old and new invocations from overlapping. We define an executor-level installation contract combining expected installation identity, invocation quiescence, stateless admission, authority revalidation, and resource-atomic publication. A prototype implements this contract for one opt-in runtime slot using a nonblocking transition gate. We compare it with atomic publication sharing the same manager checks, accounting, and receipts, under matched failures and concurrent invocation. Across nine matched scenarios, atomic publication exposes one old/new-guard overlap; guarded replacement exposes none. Both preserve the predecessor on six rejecting requests. Hosted measurements separately quantify replacement and guard costs, retries, skipped dispatches, and retained bytecode. A deterministic payload-shift replay separately measures tracking quality and installation defects under a frozen gain-update sequence. Foundation-model adaptation, downstream command fencing, and physical safety remain outside the evaluated contract.
 \end{abstract}
 
 \section{The update is part of the execution contract}
@@ -27,23 +27,23 @@ Qin et al. govern capability versions through compatibility checks, sandbox/shad
 
 Action assurance is complementary: shielding constrains learner actions \citep{alshiekh2018shielding}, while Neural Simplex combines runtime assurance and online retraining \citep{phan2020neural}. An action monitor asks whether a command may execute; publication determines which installation may issue it. Evaluation should distinguish these obligations.
 
-Our contributions are: (1) an executor-level contract combining expected installation identity, invocation quiescence, state admissibility, authority revalidation, and resource-atomic publication; (2) an implementation for one exclusive slot using a nonblocking transition gate and monotonic epochs; and (3) a comparison with atomic publication sharing the same manager checks, reporting correctness and hosted availability costs separately.
+Our contributions are: (1) an executor-level contract combining expected installation identity, invocation quiescence, state admissibility, authority revalidation, and resource-atomic publication; (2) an implementation for one exclusive slot using a nonblocking transition gate and monotonic epochs; and (3) a comparison with atomic publication sharing the same manager checks, reporting correctness and hosted availability costs separately; and (4) a simulated payload-shift replay separating task quality from installation correctness.
 
 \section{A narrow transactional publication primitive}
 \label{sec:semantics}
-The prototype is a bare-metal Rust research kernel with verified runtime programs, authenticated load policy, capabilities, quotas, generation-tagged handles, and immutable snapshots. We add one opt-in exclusive TIMER slot. Ordinary TIMER fanout coexists outside its gate: exclusivity concerns this slot, not all hardware access. Here, \emph{transactional} means publication of software authority, without atomic physical effects or persistent recovery.
+A bare-metal Rust research kernel supplies verified runtime programs, load authentication, capabilities, quotas, and immutable snapshots. We add one opt-in exclusive TIMER slot. Ordinary TIMER fanout remains outside its gate; exclusivity concerns this slot. \emph{Transactional} denotes software-authority publication, without atomic physical effects or persistent recovery.
 
 An installation is identified by
 \begin{equation}
  I=(h,e), \qquad e\in\{1,\ldots,2^{64}-1\},
 \end{equation}
-where $h$ is a program handle and $e$ is a monotonically increasing installation epoch. Replacement is a compare-and-swap-style operation on the expected $I$, not just $h$. It publishes a fresh epoch at every successful replacement, including rollback, so removing and later reinstalling the same handle does not make an old request current (the ABA case). Epoch exhaustion rejects the transition. Bootstrap is a separate operation.
+where $h$ is a program handle and $e$ increases at every successful replacement, including rollback. Replacement checks the expected full $I$, preventing a stale request from becoming current when the same handle is reinstalled (ABA). Epoch exhaustion rejects; bootstrap is separate.
 
-Invocation and replacement share one atomic state: open, active, or transitioning. An invocation atomically claims the open state; a transitioning state returns \textsc{TransitionBusy}, and an active state returns \textsc{ExecutionBusy}. Both failures increment explicit skipped-invocation counters. The invocation holds the immutable snapshot guard throughout execution and drops that guard before reopening the slot. A replacement tries to change open to transitioning once. If an invocation is active, it returns \textsc{Busy} immediately and leaves the slot unchanged; it never waits for quiescence.
+Invocation and replacement share one atomic state: open, active, or transitioning. Invocation claims open$\rightarrow$active, holds its snapshot guard throughout execution, and drops it before reopening. Other entry attempts return \textsc{TransitionBusy} or \textsc{ExecutionBusy} and increment skip counters. Replacement tries open$\rightarrow$transitioning once; an active invocation causes immediate, unchanged \textsc{Busy} rejection.
 
-The supported policy is \textsc{Stateless}: candidates must have no verifier-reported runtime-map references. Map-bearing candidates and state transfer are rejected before gate acquisition. The internal API retains the enum name \texttt{Reset}, but performs no clearing. This restriction says nothing about physical, device, or external state.
+\textsc{Stateless} admits only candidates without verifier-reported runtime-map references. Map-bearing candidates and state transfer reject before gate acquisition. The internal enum \texttt{Reset} performs no clearing and makes no claim about device or external state.
 
-Once gated and quiescent, replacement checks expected identity, ownership, retained load-time authentication provenance, and candidate authorization against the ceiling captured at bootstrap. It reruns TIMER-context verification and checks conflicts and quota; it does not reauthenticate the artifact. Fallible snapshot preparation precedes live mutation. A final emergency-stop check uses the kernel's existing monitor serialization; stop state or lock contention rejects without change. The published immutable object contains both the runtime reference and full installation identity. The serialized manager commits an allocation-free CPU-admission delta of $-\mathrm{cost}(A)+\mathrm{cost}(B)$, publishes that object once, and records the matching receipt before reopening the slot. Invocation readers consume the object; other manager operations cannot observe intermediate ledger or receipt state. Resident-memory charges for both programs persist. Replacement leaves the previous program loaded. Rollback uses the same conditional replacement in reverse with a fresh epoch, provided that program has not subsequently been unloaded.
+Once quiescent, replacement checks expected identity, ownership, retained authentication provenance, bootstrap authority ceiling, TIMER re-verification, conflicts, and quota. It does not reauthenticate. Fallible snapshot preparation precedes mutation; a final serialized emergency-stop check rejects stop state or lock contention unchanged. The immutable published object contains runtime and full identity. The manager commits an allocation-free admission delta $-\mathrm{cost}(A)+\mathrm{cost}(B)$, publishes once, and records the receipt before reopening. Invocations read the object; serialized manager operations cannot observe intermediate ledger or receipt state. Both programs retain resident-memory charges. Rollback conditionally reinstalls the resident predecessor with a fresh epoch.
 
 \paragraph{Two invariants.}
 Assume serialized manager operations, normal returns, and invocations that hold their slot guard throughout their body. \emph{Invocation exclusivity:} if replacement from $I_A$ to $I_B$ succeeds, no guarded invocation of $I_A$ overlaps one of $I_B$. Entry claims open$\rightarrow$active; replacement claims open$\rightarrow$transitioning on the same atomic state. These claims exclude one another. Replacement publishes $I_B$ while transitioning, rejects new entries until completion, and reopens only with $I_B$ current. An old reader drops its snapshot before reopening. A successful invocation linearizes at its open-to-active transition; replacement linearizes at the pointer swap of the complete installation object.
@@ -54,16 +54,13 @@ Receipts and diagnostic guard observations carry installation identity; producti
 
 \section{Correctness and availability under matched updates}
 \label{sec:evaluation}
-\paragraph{Two evaluation axes.}
-The task axis measures whether activated candidates improve a declared task metric; the installation axis checks publication correctness. Improvement cannot excuse an installation defect, and correct publication cannot establish adaptation quality. Fixed candidate sequences isolate the installation axis.
-
 \paragraph{Same-manager comparison.}
-The primary protocols are \textbf{atomic publication} (AP) and \textbf{guarded replacement} (GR). They share candidate validation, full expected identity, ownership and authority checks, TIMER re-verification, admission-ledger replacement, snapshot preparation, and receipts. AP bypasses only the invocation gate, through a host-only diagnostic path absent from bare-metal builds. Both publish the same immutable identity/runtime object. AP's epoch mechanism retains the predecessor allocation and waits for old readers before returning, but B becomes visible during that wait. A reader of B can therefore overlap a retained reader of A before replacement returns. GR instead returns \textsc{Busy} while A is active; after A finishes, a retry can publish B. Each fixture fixes its protocol until draining cleanup.
+\textbf{Atomic publication} (AP) and \textbf{guarded replacement} (GR) share validation, expected identity, ownership, authority, re-verification, accounting, snapshot preparation, and receipts. AP bypasses only the invocation gate through a host diagnostic path absent from bare-metal builds. Both publish the same immutable identity/runtime object. AP retains the predecessor allocation and waits for old readers before returning, but B becomes visible during that wait. A B reader can therefore overlap an A reader. GR returns \textsc{Busy} while A is active; a later retry can publish B. Each fixture fixes its protocol until draining cleanup.
 
-The hosted campaign uses hand-written map-free bytecode and unsigned development loading; it does not test authentication. Real snapshot/slot guards are held through observation callbacks, without executing privileged bytecode or actuation helpers. The admission profile assigns a nominal 1\,kHz hook frequency and a 500\,ms/s CPU budget; unrelated syscall-hook reservations exercise the finite budget. Fixture charges are pre-measured through standalone manager attachments. These charges are admission-model quantities, not measured execution times. A separate QEMU probe checks interpreter dispatch after A--B--A installation and stale-request rejection.
+Hand-written map-free bytecode uses unsigned development loading, so authentication is not tested. Hosted callbacks hold real guards without privileged bytecode execution or actuation. The admission model assigns 1\,kHz hook frequency and 500\,ms/s CPU budget; unrelated reservations exercise that budget. Fixture charges are measured through manager attachments, not execution timing. A separate QEMU probe checks interpreter dispatch after A--B--A and stale-request rejection.
 
 \paragraph{Fault schedules.}
-Both protocols receive the same snapshot-preparation failure, over-budget candidate, authority violation, stale expected installation, completed retry, ABA request, rollback, and concurrent-proposer schedules. Snapshot, full identity, ledger, and receipt are checked before and after returned errors. The concurrent-invocation schedule retains an A guard, starts replacement, and attempts a B invocation. AP must expose B before releasing A; GR rejects replacement until A drains. Overlap observations carry the identities captured by the actual guards. We do not read manager accounting from a callback while AP's writer waits for that callback, which would deadlock; accounting is checked before and after the operation.
+Both protocols receive snapshot-preparation failure, over-budget, authority, stale-identity, retry, ABA, rollback, and concurrent-proposer schedules. Rejections preserve snapshot, identity, ledger, and receipt. One schedule retains A, starts replacement, and attempts a B invocation. AP exposes B before A drains; GR rejects until A drains. Overlap records carry identities captured by actual guards. Accounting is checked outside callbacks: taking the manager lock while AP waits for a reader would deadlock.
 
 \begin{table}[t]
 \centering
@@ -76,7 +73,7 @@ Both protocols receive the same snapshot-preparation failure, over-budget candid
 Attach-first and detach-first remain supporting failure witnesses: stopping between calls exposes overlap or an empty slot, without simulating a crash inside commit. Python recomputes cardinality, identity, overlap, and ledger invariants from raw observations. Same-source Loom and manager tests separately check guard lifetime and rejection paths.
 
 \paragraph{Hosted cost campaign.}
-We use release builds with identical preloaded candidates across both manager paths. A dispatcher and updater are pinned to separate physical cores of an AMD Ryzen 7 7735HS running Linux, with normal scheduling and frequency scaling. Dispatch has an absolute 1\,ms period and requested guard hold times of 0, 10, 100, or 500\,$\mu$s. Hold durations are imposed test conditions, not bytecode runtimes. Per protocol and hold regime, ten fresh processes each perform 1,000 raw replacement attempts after excluded warmup. A deterministic phase sweep spans the dispatch period. A \textsc{Busy} result retains the same logical request; only a successful receipt advances the expected installation and candidate.
+Release builds use identical preloaded candidates. Dispatcher and updater are pinned to separate physical cores of an AMD Ryzen 7 7735HS under normal Linux scheduling and frequency scaling. Dispatch period is 1\,ms; imposed guard holds are 0/10/100/500\,$\mu$s, not bytecode runtimes. Each protocol/hold regime has ten fresh processes with 1,000 attempts after excluded warmup. A deterministic phase sweep spans the period. \textsc{Busy} retains the same logical request; only success advances expected identity and candidate.
 
 \begin{table}[t]
 \centering
@@ -88,17 +85,30 @@ We use release builds with identical preloaded candidates across both manager pa
 
 \input{cost-note.tex}
 
-Timing uses \texttt{CLOCK\_MONOTONIC\_RAW}, preallocated buffers, and no JSON emission in timed regions. Clock-pair overhead is measured without subtraction. Replacement latency includes verification through reclamation; observer timing excludes BPF execution. The artifact retains per-process count/median/p99/maximum, release lateness, actual holds, missed releases, and retries. Reported latencies aggregate per-process quantiles; missing populations are undefined. First-try fractions count logical requests started; skips/update uses successful activations.
+Timing uses \texttt{CLOCK\_MONOTONIC\_RAW}, preallocated buffers, and no timed JSON emission. Clock overhead is retained without subtraction. Latencies include verification through reclamation; observer costs exclude BPF execution. Raw per-process count/median/p99/maximum, lateness, holds, missed releases, and retries are retained. First-try fractions count logical requests started; skips/update uses successful activations.
 
-Reproduction retains revision/patch, source and executable hashes, commands, raw JSONL, logs, environment, CSV/LaTeX, and checksums. One command rebuilds the hosted campaign and tables; cross-compilation and QEMU provide separate software checks.
+\paragraph{Installation-conditioned control replay.}
+An eight-second host simulation integrates $m\dot v=u-v$ by 100\,$\mu$s Euler steps, with mass $1\rightarrow2$ at four seconds and reference alternating $\pm1$ each second. Control ticks every millisecond use $u=\mathrm{clip}(k(r-v),-4,4)$. A seed tuner, starting at $k=1$, doubles gain up to 16 when recent 250\,ms tracking RMSE exceeds 0.1, at 4.25, 4.5, 4.75, and 5 seconds. We freeze these proposals before replaying frozen control, AP, and GR.
+
+Real manager installations and guards select the gain; host code computes control. Each invocation captures identity and error at entry and applies its command at completion, with zero-order hold between commands. Normal holds are 100\,$\mu$s; one predecessor invocation at 4.249\,s lasts 2\,ms. Completion precedes update attempts, then control ticks. Each proposal has the same primary and $+1$\,ms attempt; AP can expose B while A remains held, whereas GR must retry. RMSE includes skipped ticks. Paired synchronous utility probes compare candidate and predecessor gains from the same activation state over 250\,ms; they do not estimate a learning algorithm's performance.
+
+\begin{table}[t]
+\centering
+\small
+\input{adaptation.tex}
+\caption{Deterministic control replay. Tracking quality and installation defects are distinct axes. Frozen control uses the guarded slot without updates; commanded motion is simulated.}
+\label{tab:adaptation}
+\end{table}
+
+Reproduction retains revision/patch, source and executable hashes, commands, raw traces, environment, generated tables, and checksums.
 
 \section{Scope and implications}
 \label{sec:limits}
-Quiescence trades availability for rejection and serializes invocations. Acceptability depends on the controller and schedule; maximal rejection is not success. Retaining predecessors costs memory, and stateless admission excludes useful updates. Equivalent middleware could enforce this contract. Our AP baseline is a manager ablation, not LITHE: a single loop can provide cycle-boundary quiescence without this gate.
+Quiescence serializes invocations and trades availability for rejection; acceptability depends on the controller and schedule. Retaining predecessors costs memory; stateless admission excludes useful updates. Middleware could enforce this contract. AP is a manager ablation, not LITHE, whose single loop supplies cycle-boundary quiescence.
 
 
 
-The contract ends at the guarded invocation. Commands can outlive their installation; physical motion cannot be rolled back. Queue fencing requires propagated identity and a stale-command rejection point. Controller stability, state transfer, persistent recovery, hardware-stop timing, and continual-learning quality require separate experiments. Motor-reflex and sensor-processing adaptation should report task quality independently from installation correctness: adaptation should not imply deployment.
+The contract ends at the guarded invocation. Commands can outlive their installation; physical motion cannot be rolled back. Queue fencing requires propagated identity and a stale-command rejection point. Controller stability, state transfer, persistent recovery, hardware-stop timing, and foundation-model continual learning require separate experiments. The replay illustrates two evaluation axes, without a robot or privileged controller execution: adaptation should not imply deployment.
 
 \clearpage
 \bibliographystyle{plainnat}
diff --git c/papers/cl4fmagents2026/verify.py w/papers/cl4fmagents2026/verify.py
index a17654f..2704da9 100644
--- c/papers/cl4fmagents2026/verify.py
+++ w/papers/cl4fmagents2026/verify.py
@@ -17,7 +17,59 @@ def output(*args):
     return subprocess.check_output(args, text=True)
 
 
+def check_capture(evidence, markers, names):
+    checksums = {}
+    for line in (evidence / "SHA256SUMS").read_text().splitlines():
+        digest, separator, name = line.partition("  ")
+        relative = Path(name)
+        assert separator and re.fullmatch(r"[0-9a-f]{64}", digest), "Malformed checksum"
+        assert name and not relative.is_absolute() and ".." not in relative.parts
+        assert relative.as_posix() == name and name not in checksums, "Duplicate/aliased checksum path"
+        path = evidence / relative
+        assert not path.is_symlink() and path.is_file(), f"Missing/linked evidence: {name}"
+        assert hashlib.sha256(path.read_bytes()).hexdigest() == digest, f"Corrupt evidence: {name}"
+        checksums[name] = digest
+    assert set(checksums) == {str(p.relative_to(evidence)) for p in evidence.rglob("*")
+                              if p.is_file() and p.name != "SHA256SUMS"}, "Incomplete checksums"
+    manifest = json.loads((evidence / "environment.json").read_text())
+    assert manifest["exit_code"] == 0 and not manifest["timed_out"], "Capture failed"
+    assert not manifest["malformed_records"], "Malformed raw evidence"
+    assert not manifest["changed_sources_during_run"] and not manifest["changed_executables_during_run"]
+    source_digest = hashlib.sha256(json.dumps({"revision": manifest["revision"],
+        "sources": manifest["source_sha256"]}, sort_keys=True).encode()).hexdigest()
+    assert source_digest == manifest["source_digest"] == manifest["build_source_digest"] == manifest["finished_source_digest"]
+    assert manifest["patch_sha256"] == checksums["source.patch"]
+    for name, digest in manifest["source_sha256"].items():
+        retained = "untracked-source/" + name
+        if retained in checksums:
+            assert checksums[retained] == digest, f"Source copy disagrees with manifest: {name}"
+    for marker, trace in markers:
+        assert manifest["trace_records"][marker] == sum(1 for line in (evidence / trace).open() if line.strip())
+    build_command = json.loads((evidence / "build-command.json").read_text())
+    assert "--locked" in build_command and "--release" in build_command
+    assert "kernel_bpf/embedded-profile" in build_command[build_command.index("--features") + 1]
+    targets = {}
+    for line in (evidence / "build-artifacts.jsonl").read_text().splitlines():
+        record = json.loads(line)
+        if record.get("reason") == "compiler-artifact" and record.get("executable"):
+            targets[record["target"]["name"]] = record["executable"]
+    assert set(names) <= targets.keys()
+    assert {targets[name] for name in names} == set(manifest["executable_sha256"])
+    if os.environ.get("UPDATE_PUBLICATION_CHECK_BINARIES") == "1":
+        for name, digest in manifest["executable_sha256"].items():
+            assert hashlib.sha256(Path(name).read_bytes()).hexdigest() == digest, f"Measured binary changed: {name}"
+
+
 pdf = HERE / "who-guards-the-update.pdf"
+build_inputs = {}
+for line in (HERE / "build/inputs.sha256").read_text().splitlines():
+    digest, name = line.split("  ", 1)
+    assert name not in build_inputs and Path(name).name == name
+    assert hashlib.sha256((HERE / name).read_bytes()).hexdigest() == digest, f"Stale build: {name}"
+    build_inputs[name] = digest
+assert set(build_inputs) == {"Makefile", pdf.name} | {
+    p.name for pattern in ("*.tex", "*.bib", "*.sty") for p in HERE.glob(pattern)
+}, "Build input inventory changed; rebuild the paper"
 text = output("pdftotext", "-layout", str(pdf), "-")
 pages = [page for page in text.split("\f") if page.strip()]
 reference_pages = [i for i, page in enumerate(pages)
@@ -41,46 +93,8 @@ source = (HERE / "main.tex").read_text()
 assert r"\usepackage[dblblindworkshop]{neurips_2026}" in source
 evidence = Path(os.environ.get("UPDATE_PUBLICATION_EVIDENCE",
                 ROOT / "docs/performance/evidence/update-transaction-v2"))
-checksums = {}
-for line in (evidence / "SHA256SUMS").read_text().splitlines():
-    digest, separator, name = line.partition("  ")
-    relative = Path(name)
-    assert separator and re.fullmatch(r"[0-9a-f]{64}", digest), "Malformed checksum"
-    assert name and not relative.is_absolute() and ".." not in relative.parts
-    assert relative.as_posix() == name and name not in checksums, "Duplicate/aliased checksum path"
-    path = evidence / relative
-    assert not path.is_symlink() and path.is_file(), f"Missing/linked evidence: {name}"
-    assert hashlib.sha256(path.read_bytes()).hexdigest() == digest, f"Corrupt evidence: {name}"
-    checksums[name] = digest
-assert set(checksums) == {str(p.relative_to(evidence)) for p in evidence.rglob("*")
-                          if p.is_file() and p.name != "SHA256SUMS"}, "Incomplete checksums"
-manifest = json.loads((evidence / "environment.json").read_text())
-assert manifest["exit_code"] == 0 and not manifest["timed_out"], "Capture failed"
-assert not manifest["malformed_records"], "Malformed raw evidence"
-assert not manifest["changed_sources_during_run"] and not manifest["changed_executables_during_run"]
-source_digest = hashlib.sha256(json.dumps({"revision": manifest["revision"],
-    "sources": manifest["source_sha256"]}, sort_keys=True).encode()).hexdigest()
-assert source_digest == manifest["source_digest"] == manifest["build_source_digest"] == manifest["finished_source_digest"]
-assert manifest["patch_sha256"] == checksums["source.patch"]
-for name, digest in manifest["source_sha256"].items():
-    retained = "untracked-source/" + name
-    if retained in checksums:
-        assert checksums[retained] == digest, f"Source copy disagrees with manifest: {name}"
-for marker, trace in (("UPDATE_TXN", "trace.jsonl"), ("UPDATE_COST", "cost-trace.jsonl")):
-    assert manifest["trace_records"][marker] == sum(1 for line in (evidence / trace).open() if line.strip())
-build_command = json.loads((evidence / "build-command.json").read_text())
-assert "--locked" in build_command and "--release" in build_command
-assert "kernel_bpf/embedded-profile" in build_command[build_command.index("--features") + 1]
-targets = {}
-for line in (evidence / "build-artifacts.jsonl").read_text().splitlines():
-    record = json.loads(line)
-    if record.get("reason") == "compiler-artifact" and record.get("executable"):
-        targets[record["target"]["name"]] = record["executable"]
-assert {"bpf_update_campaign", "bpf_update_measurements"} <= targets.keys()
-assert {targets[name] for name in ("bpf_update_campaign", "bpf_update_measurements")} == set(manifest["executable_sha256"])
-if os.environ.get("UPDATE_PUBLICATION_CHECK_BINARIES") == "1":
-    for name, digest in manifest["executable_sha256"].items():
-        assert hashlib.sha256(Path(name).read_bytes()).hexdigest() == digest, f"Measured binary changed: {name}"
+check_capture(evidence, (("UPDATE_TXN", "trace.jsonl"), ("UPDATE_COST", "cost-trace.jsonl")),
+              ("bpf_update_campaign", "bpf_update_measurements"))
 # Verification must never rewrite the retained evidence being checked.
 with tempfile.TemporaryDirectory(prefix="publication-paper-check-") as directory:
     regenerated = Path(directory)
@@ -98,12 +112,24 @@ analysis = importlib.util.module_from_spec(spec)
 spec.loader.exec_module(analysis)
 reported = json.loads((evidence / "analysis.json").read_text())
 assert analysis.analyze(records) == {**reported["paired_summary"], **reported["supporting_summary"]}
-for local, retained in (("results.tex", "result-table.tex"), ("costs.tex", "cost-table.tex"),
-                        ("cost-note.tex", "cost-note.tex")):
-    assert (HERE / local).read_bytes() == (evidence / retained).read_bytes()
+adaptation = Path(os.environ.get("UPDATE_ADAPTATION_EVIDENCE",
+                  ROOT / "docs/performance/evidence/update-adaptation-v1"))
+check_capture(adaptation, (("UPDATE_ADAPT", "adaptation-trace.jsonl"),), ("bpf_update_adaptation",))
+with tempfile.TemporaryDirectory(prefix="adaptation-paper-check-") as directory:
+    regenerated = Path(directory)
+    subprocess.run(["python3", str(ROOT / "scripts/benchmark/analyze-update-adaptation.py"),
+                    str(adaptation / "adaptation-trace.jsonl"), "--output-dir", str(regenerated)],
+                   check=True, capture_output=True, text=True, timeout=120)
+    for name in ("analysis.json", "adaptation-table.tex"):
+        assert (adaptation / name).read_bytes() == (regenerated / name).read_bytes(), name
+for local, retained, directory in (("results.tex", "result-table.tex", evidence),
+                                   ("costs.tex", "cost-table.tex", evidence),
+                                   ("cost-note.tex", "cost-note.tex", evidence),
+                                   ("adaptation.tex", "adaptation-table.tex", adaptation)):
+    assert (HERE / local).read_bytes() == (directory / retained).read_bytes()
     assert f"\\input{{{local}}}" in source, f"PDF does not include {local}"
     for row in (HERE / local).read_text().splitlines():
-        if " & " not in row or row.startswith(("Observation &", "Fault &", "Hold ")):
+        if " & " not in row or row.startswith(("Observation &", "Fault &", "Hold ", "Protocol &", "Metric &")):
             continue
         cells = row.removesuffix(r" \\").split(" & ")
         cells = [re.sub(r"\\(?:textsc|textbf|texttt)\{([^{}]*)\}", r"\1", cell) for cell in cells]
diff --git c/scripts/benchmark/reproduce-update-publication.py w/scripts/benchmark/reproduce-update-publication.py
index 02ca024..ec472ff 100644
--- c/scripts/benchmark/reproduce-update-publication.py
+++ w/scripts/benchmark/reproduce-update-publication.py
@@ -39,6 +39,9 @@ def main():
     destination = args.output.resolve()
     if destination.exists():
         parser.error("output already exists; choose a new directory")
+    adaptation = destination.with_name(destination.name + "-adaptation")
+    if adaptation.exists():
+        parser.error("paired adaptation output already exists; choose a new directory")
     spec = importlib.util.spec_from_file_location("publication_runner",
         ROOT / "scripts/benchmark/update-transaction-runner.py")
     collector = importlib.util.module_from_spec(spec)
@@ -77,9 +80,13 @@ def main():
     shutil.copyfile(destination / "result-table.tex", paper / "results.tex")
     shutil.copyfile(destination / "cost-table.tex", paper / "costs.tex")
     shutil.copyfile(destination / "cost-note.tex", paper / "cost-note.tex")
+    run([sys.executable, ROOT / "scripts/benchmark/reproduce-update-adaptation.py",
+         "--output", adaptation])
+    shutil.copyfile(adaptation / "adaptation-table.tex", paper / "adaptation.tex")
     run(["make", "-C", paper])
     run([sys.executable, paper / "verify.py"],
         env={**os.environ, "UPDATE_PUBLICATION_EVIDENCE": str(destination),
+             "UPDATE_ADAPTATION_EVIDENCE": str(adaptation),
              "UPDATE_PUBLICATION_CHECK_BINARIES": "1"})
 
 
diff --git c/scripts/benchmark/update-transaction-runner.py w/scripts/benchmark/update-transaction-runner.py
index affaced..d4067c5 100644
--- c/scripts/benchmark/update-transaction-runner.py
+++ w/scripts/benchmark/update-transaction-runner.py
@@ -17,7 +17,7 @@ import threading
 from pathlib import Path
 
 ROOT = Path(__file__).resolve().parents[2]
-MARKERS = {"UPDATE_TXN": "trace.jsonl", "UPDATE_COST": "cost-trace.jsonl"}
+MARKERS = {"UPDATE_TXN": "trace.jsonl", "UPDATE_COST": "cost-trace.jsonl", "UPDATE_ADAPT": "adaptation-trace.jsonl"}
 
 
 def git(*args: str) -> bytes:
