<!-- Generated by scripts/unsafe-ledger.py; edit unsafe-ledger.toml instead. -->
# Unsafe-code ledger

This ledger turns every first-party Rust `unsafe` syntax site into an owned review obligation. It covers tracked and untracked Rust source, including tests and fuzz targets, while excluding build output, generated source, fuzz corpora, and fuzz artifacts. Counts are syntactic source sites, not expanded macro instances or a proof of soundness.

Run `python3 scripts/unsafe-ledger.py --check` before review. Run `python3 scripts/unsafe-ledger.py --list` for the site-level TSV containing location, construct, owner, invariant, callers, tests, priority, and review date. Update [unsafe-ledger.toml](unsafe-ledger.toml) when an invariant, owner, or baseline changes, then run `python3 scripts/unsafe-ledger.py --write`.

Current snapshot: **656 sites in 119 files** (attribute 146, block 409, extern 25, function 55, impl 20, trait 1). Inventory fingerprint: `sha256:c9048762e517b41c4197c861d38471cd61168a86021ef8df6e74ac78d472cf97`. All sites are classified by the obligations below.

| Obligation | Sites | Scope | Owner | Priority | Required invariant | Principal callers | Executable evidence | Reviewed |
|---|---:|---|---|---|---|---|---|---|
| TEST-HARNESS | 76 | test/tooling | owning crate maintainers | P3 | Synthetic pointers, ABI shims, and reset hooks remain confined to process-isolated test or fuzz binaries. | cargo test, cargo bench, cargo fuzz | The containing harness is the evidence; production linkage must remain impossible. | 2026-07-12 |
| MEM-ADDRESS-SPACE | 35 | production | memory/MMU maintainers | P0 | Page-table pointers are uniquely mapped and live under the address-space lock; user copies validate every page with permissions held stable. | process creation, exec, mmap/munmap, CurrentUserMemory | kernel_usermem tests; x86/AArch64 kernel checks; QEMU user-copy and boot smoke. Concurrency fault injection remains missing. | 2026-07-12 |
| MEM-PHYSICAL | 14 | production | physical-memory maintainers | P0 | Frame allocator globals are initialized once, serialized, and never hand out aliased or reserved frames. | heap setup, page-table mapper, DMA allocation, AddressSpace | kernel_physical_memory unit tests and boot smoke; SMP allocator stress and reserved-range fault injection are missing. | 2026-07-12 |
| MEM-VIRTUAL | 3 | production | virtual-memory maintainers | P0 | Released virtual segments were allocated by the same allocator, remain unmapped, and cannot be released twice or overlap live reservations. | address spaces, heap, MMIO and JIT mappings | kernel_virtual_memory unit tests; transactional map/unmap and double-release integration tests remain missing. | 2026-07-12 |
| MEM-KERNEL-RAW | 9 | production | kernel memory maintainers | P1 | Raw allocation and slice construction use live mapped allocations with exclusive access for mutable views and exact layout provenance. | global allocator, kernel collections, mapper bookkeeping | Host crate tests and boot smoke; Miri coverage for host-capable allocation primitives remains missing. | 2026-07-12 |
| SYSCALL-BOUNDARY | 10 | production | syscall/security maintainers | P0 | Syscall register values are treated as untrusted integers; memory access crosses only the bounded user-memory adapter while the mapping guard is held. | architecture syscall entry and dispatch | kernel_usermem/kernel_syscall tests and QEMU invalid-pointer smoke; full syscall fault matrix remains missing. | 2026-07-12 |
| SYSCALL-PRIMITIVES | 28 | production | syscall/security maintainers | P0 | UserspacePtr construction proves only numeric range; dereference requires a UserMemory implementation that validates mappings, permissions, bounds, and lifetime. | kernel syscall adapters and process memory operations | kernel_syscall and kernel_usermem unit tests; targeted Miri is required for host-capable pointer helpers. | 2026-07-12 |
| SCHEDULER-WAIT-TRANSITION | 1 | production | scheduler/SMP maintainers | P0 | The current task owns a generation-stamped wait registration, the condition lock is released, and local interrupts remain masked across the exclusive scheduler transition. | child-exit, pipe, and device wait paths | wait protocol interleaving model, x86/AArch64 kernel checks, and SMP QEMU lifecycle smoke | 2026-07-14 |
| SCHEDULER-TASK | 59 | production | scheduler/SMP maintainers | P0 | Task and CPU-local state has one mutable owner at a time; context pointers stay pinned and live across switches; interrupt state matches lock assumptions. | timer interrupt, syscall exit, spawn/exit/wait, scheduler switch | QEMU SMP boot and lifecycle smoke; loom/model tests and forced preemption at mutation points remain missing. | 2026-07-12 |
| AARCH64-PAGING | 38 | production | AArch64 MMU maintainers | P0 | Descriptor writes target aligned, uniquely owned tables; physical-to-virtual translations refer to mapped RAM; TLB maintenance orders publication and reuse. | AArch64 boot, AddressSpace, heap/MMIO mapping | AArch64 compile and QEMU boot smoke; map/unmap fault injection and multicore TLB shootdown tests remain missing. | 2026-07-12 |
| AARCH64-PLATFORM-MMIO | 48 | production | AArch64 platform/driver maintainers | P1 | MMIO addresses match the selected board DT/platform map, register access is volatile and width-correct, and peripheral ownership is serialized. | Raspberry Pi 5 and virt platform initialization, GPIO/PWM/UART control | Platform compile and hardware/QEMU smoke; register-level mocks and concurrent peripheral ownership tests are incomplete. | 2026-07-12 |
| AARCH64-EXCEPTION-CONTEXT | 85 | production | AArch64 architecture maintainers | P0 | Assembly context layouts match Rust repr(C) types, exception vectors preserve all required registers, and interrupt-controller state is CPU-local and initialized. | AArch64 boot, exception vectors, scheduler, syscall entry | AArch64 compile and QEMU boot/exception smoke; nested exception and SMP interrupt stress remain missing. | 2026-07-12 |
| X86-INTERRUPT-CONTEXT | 25 | production | x86_64 architecture maintainers | P0 | Naked/assembly frames match Rust layouts, privilege transitions use valid stacks/selectors, interrupt state is restored, and MMIO timer/controller mappings stay live. | x86 boot, IDT/syscall entry, scheduler tick, timekeeping | x86 kernel check and QEMU boot/syscall/exception smoke; nested interrupt and malformed-frame tests remain missing. | 2026-07-12 |
| RISCV-ARCH | 2 | experimental demo | RISC-V architecture maintainers | P1 | Trap/context assembly matches Rust layouts, CSR and SBI operations execute at the expected privilege level, and boot pointers are valid for the machine profile. | RISC-V boot, trap entry, shutdown and demo runtime | RISC-V target clippy/check; boot, syscall, and trap integration coverage is incomplete. | 2026-07-12 |
| BPF-HELPERS | 31 | production | BPF runtime/security maintainers | P0 | Helper arguments proven by the verifier still refer to live context, map, and stack regions of the required size and alignment at execution time. | BPF interpreter and JIT helper dispatch | cloud/embedded BPF suites and semantic consistency tests; adversarial helper provenance and concurrent map-lifetime tests remain required. | 2026-07-12 |
| BPF-EXECUTION-JIT | 45 | production | BPF runtime/JIT maintainers | P0 | Only VerifiedProgram reaches execution; context lifetime covers execution; generated code obeys ABI, branch, bounds, cache, and W^X requirements. | BpfExecutor, kernel hook dispatch, AArch64 JIT | cloud/embedded execution and verifier suites plus fuzzing; JIT differential tests and enforced RW-to-RX lifecycle remain incomplete. | 2026-07-12 |
| BPF-MAPS | 9 | production | BPF map/runtime maintainers | P0 | Returned map pointers remain aligned, in-bounds, initialized, and live for the full access; Send/Sync premises hold under concurrent update and unload. | BPF helpers, actuation controller, userspace map syscalls | cloud/embedded map suites and property tests; Miri/loom and unload-during-execution tests remain missing. | 2026-07-12 |
| BPF-KERNEL-INTEGRATION | 10 | production | BPF runtime/security maintainers | P0 | Verifier, profile, attach, scheduler, signing, and kernel registry boundaries preserve program, context, credential, quota, and lifetime invariants. | bpf syscall, device hooks, scheduler hooks, actuation | cloud/embedded complete BPF suites, epoch-snapshot concurrency tests, SMP attach/fire/detach QEMU stress, and kernel checks; model-checked reclamation remains missing. | 2026-07-13 |
| DMA-MMIO-DRIVERS | 27 | production | device/PCI/VirtIO maintainers | P1 | MMIO and DMA pointers are mapped for the device lifetime, buffers satisfy device ownership/alignment rules, and transport access is serialized. | ACPI/PCI discovery, VirtIO init, block/GPU/raw device I/O | QEMU device boot and filesystem smoke; IOMMU/DMA fault injection and hot-unplug tests are missing. | 2026-07-12 |
| FILESYSTEM-INTERIOR | 13 | production | VFS/filesystem maintainers | P1 | Interior raw references do not outlive nodes or aliases, and foreign filesystem callbacks receive valid buffers with exclusive mutation. | file-descriptor operations, mount, ext2/devfs/path resolution | VFS/devfs host tests and QEMU root-mount smoke; concurrent close/read/unmount tests remain missing. | 2026-07-12 |
| KERNEL-BOOT-RUNTIME | 40 | production | kernel platform maintainers | P1 | Bootloader/firmware pointers, linker symbols, allocator setup, assembly reads, and ABI attributes satisfy their documented platform lifetime and layout contracts. | architecture entrypoints, panic/logging, benchmark and backtrace paths | x86/AArch64/RISC-V target checks and boot smoke; malformed boot metadata and backtrace-chain tests are incomplete. | 2026-07-12 |
| ACTUATION-MMIO | 1 | production | actuation/Raspberry Pi maintainers | P1 | The RP1 GPIO register window is mapped for the kernel lifetime and the singleton peripheral is not accessed through competing mutable owners. | actuation initialization and watchdog-controlled GPIO paths | AArch64 target check and Raspberry Pi safety smoke; concurrent ownership and MMIO fault injection remain missing. | 2026-07-12 |
| USERSPACE-ABI | 45 | production/demo | userspace/runtime maintainers | P1 | Syscall assembly and exported entrypoints obey the kernel ABI; raw event/ring buffers are aligned, initialized, bounded, and live while viewed. | userspace binaries, minilib, rk_bridge | Standalone workspace checks and QEMU userspace smoke; Miri for host-capable parsers and malformed ring/event inputs is incomplete. | 2026-07-12 |
| FIRMWARE-MMIO | 1 | production/firmware | Shrike firmware maintainers | P1 | Peripheral singleton pointers are accessed with exclusive ownership and interrupt-safe synchronization for the firmware lifetime. | RP2040 control, motor, timer, and panic paths | thumbv6m target clippy/check; hardware concurrency and timer wraparound tests remain missing. | 2026-07-12 |
| ELF-BYTE-VIEW | 1 | production | ELF loader maintainers | P1 | Typed ELF table views are aligned, fully in-bounds, correctly sized, and contain only values valid for the target zerocopy type. | ElfFile section/program/symbol parsing and exec | kernel_elfloader malformed-input suite and parse/load fuzz target; cross-endian ELF input is unsupported by contract. | 2026-07-12 |

## Review policy

- `P0` obligations gate release until their premises are enforced in code and exercised through a fault or concurrency path. `P1` requires targeted tests before the owning subsystem is considered complete. `P2` is platform or ABI glue with documented external premises. `P3` is test/tooling-only syntax.
- The path rule is the durable owner of an obligation; `--list` resolves it onto every current source location. A new path containing `unsafe` fails `--check` until explicitly classified.
- Count drift fails `--check`. Reducing the count still requires catalog review because removing one site can move the same obligation into a larger unsafe helper.
- “Boot smoke” is integration evidence, not a substitute for provenance, aliasing, lifetime, mapping, interrupt, DMA, or MMIO tests. Rows that explicitly say coverage is missing remain open audit debt.
