At 0~$\mu$s hold, successful observer round-trip was 200 ns (run-median range 200--662 ns) for AP and 210 ns (run-median range 210--791 ns) for GR. At 500~$\mu$s hold, GR \texttt{Busy} return latency was 130/471 ns median/p99; the separately controlled \texttt{TransitionBusy} return was 50/140 ns median/p99. The manager-accounted resident bytecode was 16 bytes for one loaded candidate and 32 bytes for two; these counters exclude allocator, metadata, and process RSS.
