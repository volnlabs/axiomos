# Final revision changelog

- Added a feature-gated, nonallocating post-swap observation to the existing manager publication path. Runtime and identity remain co-published; ledger/receipt consistency is scoped to serialized manager operations and normal return.
- Extended the hosted campaign to full scheduled/call/publication/return timing, logical Busy/retry sequences, censoring, thresholds and observed installation identities. Added 900/1100us regimes with the same predeclared policies.
- Added 400 primary and 100 separate stress replays on a common deterministic integration mesh, plus a predeclared corrective-stop pair. Independent reduction validates raw identities, commands, accounting, plant evolution, timestamps and denominators.
- Retained the legacy witness, preliminary timing capture, pilot and failed validation attempts. Complete replay recapture is byte-identical; hosted variation is not pooled or hidden.
- Rewrote the four-page body around QF-AP versus GR, added the two-panel protocol/timeline figure, explicit five-part contract and scoped proof sketches, and a five-page technical appendix.
- Replaced successful-call-only headline timing with comparable scheduled-request publication latency. Completion counts and high-occupancy censoring accompany conditional quantiles.
- Verified 14 primary references and corrected the proposed Qin metadata change: the inspected v5 has the longer lifecycle-time title and six authors including Boukhers.
- Reworked anonymous export for streamed multi-GB traces, anonymized snapshot hashes, retained reductions, neutral ZIP metadata and explicit source-subset limitations.

## Claims narrowed or removed

No new pointer-exchange/quiescence/ABA primitive; no multiword atomic ledger/receipt snapshot; no crash atomicity, persistent recovery, physical reversibility, physical safety, WCET, starvation guarantee, foundation-model learning, privileged simulated control, general state transfer or downstream command fencing. Guard coherence is not universally better control: the corrective replay favors earlier QF-AP uptake on stop time and common command exposure. At 1100us GR task differences also include steady-state same-installation serialization.

## Scope retained

One exclusive executor slot, stateless loaded candidates, existing manager checks, existing guarded/atomic baseline, hosted measurements and deterministic simulation. No hardware, broader runtime API or new model loop. All work remains local; nothing was pushed or submitted.

## Editorial follow-up

Clarified the abstract result and identified the origins of Tables 2, 6 and 7 explicitly. Appendix G distinguishes the full artifact-r3.zip from paper-source.zip. The evidence ZIP, runtime, experiments and numeric table inputs are unchanged; abstract wording was checked against the existing schedule and corrective analyses. The portable source README explicitly requires BibTeX. Previous delivered files remain preserved in the original final directory.

## Diagram clarity revision

Redrew Figure 1 with separated invocation lifetimes, aligned requests, shaded overlap and explicit Busy/retry events. Simplified gate return paths and grouped rejected operations by state. The caption defines the schematic timeline; no protocol, result or claim changed. Retained the four-page body, rebuilt the anonymous source ZIP and exported a standalone vector figure.
