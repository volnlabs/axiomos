[package]
name = "kernel_map_transaction"
version = "0.1.0"
edition = "2024"

[dependencies]
kernel_physical_memory = { path = "../kernel_physical_memory" }
kernel_virtual_memory = { path = "../kernel_virtual_memory" }

[target.'cfg(target_arch = "x86_64")'.dependencies]
x86_64.workspace = true
