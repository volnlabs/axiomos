name = "extended"
description = "Full validation plus fuzz-build, formal, and extended platform checks."
checks = ["inventory", "boundary", "docs", "quality", "host-tests", "models", "release", "fuzz", "formal", "hil"]
