[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
file_structure = { path = "../../userspace/file_structure" }
kernel_abi = { path = "../../kernel/crates/kernel_abi" }
