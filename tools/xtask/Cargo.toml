[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
clap = { version = "4.5", features = ["derive"] }
file_structure = { path = "../../userspace/core/file_structure" }
kernel_abi = { path = "../../kernel/crates/kernel_abi" }
serde = { version = "1", features = ["derive"] }
toml = "0.9"
